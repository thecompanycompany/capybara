# The Capybara Language

An experimental compiled programming language and compiler with Mojo-compatible syntax.

## Prerequisites

- LLVM 22 or newer
- CMake 4.0 or higher

## Building

```sh
cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=RelWithDebInfo
cmake --build build
```
